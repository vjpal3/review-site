<h2>Items Listing</h2>
<p></p>
<% if user_signed_in? %>
  <h4><%= link_to "Add Item", new_item_path %></h4>
<% end %>

<table>
  <% @items.each do |item| %>
    <tr>
      <td><%= link_to "#{item.name}", item_path(item) %></td>
    </tr>
  <% end %>

</table>
